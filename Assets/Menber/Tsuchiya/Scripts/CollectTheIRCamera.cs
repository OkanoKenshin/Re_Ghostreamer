using UnityEngine;

/*public class CollectTheIRCamera : MonoBehaviour
{
    [SerializeField] private float waitingTime = 5.0f;
    private Coroutine coroutineForThisClass;
    private bool inputOn;

    public void MCollectTheIRCamera()
    {
        coroutineForThisClass = StartCoroutine(WaitForCameraCollection());
    }

    private IEnumerator WaitForCameraCollection()
    {
        Debug.Log("JńűAj[VđÄś");

        yield return new WaitForSeconds(waitingTime);

        Debug.Log("JńűŽš");
    }

    public void MAbortCoroutine()
    {
        if (coroutineForThisClass != null)
        {
            StopCoroutine(coroutineForThisClass);
        }
    }
}
*/